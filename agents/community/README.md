# Community Agents

Reviewed community agent manifests live in this directory as
`<agentId>/agent.yaml`.

Community agents can reference built-in, community, or custom skills by
`skillId`, but they cannot override built-in agent IDs.
